# Generated by krkn-ai-pipeline-poc
# PoC for krkn-chaos/krkn-ai#188 — 5-stage pluggable discovery pipeline

# === Cluster Inventory ===
kubeconfig_file_path: "/home/p0ptr0n/.kube/config"
namespaces:
  - default
  - kube-node-lease
  - kube-public
  - kube-system
  - monitoring
  - stateful-demo

# Discovered: 14 pods, 17 services, 1 PVCs

# === Fitness Function ===
fitness_function:
  query: "sum(kube_pod_container_status_restarts_total)"


# === Health Checks ===
# health_checks: []   # no reachable endpoints discovered


# === Scenarios ===
scenario:
  pvc-scenarios:
    enable: true
    # reason: 1 PVC(s) detected; enabling pvc-scenarios

